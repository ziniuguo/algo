package leetcode;

public class leetcode66 {
    public int[] plusOne(int[] digits) {
        int l = digits.length;
        if (digits[l - 1] != 9) {
            digits[l - 1] = digits[l - 1] + 1;
        } else {
            digits[l-1]=0;
            plusOne(digits [])\
            digits.
        }
        return digits;
    }
}
